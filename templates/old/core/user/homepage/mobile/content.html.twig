{# {{ include(_self|replace({ "content.html.twig": "header.html.twig" })) }} #}

<main class="main">


    {# Карусель банеров  #}
     {{ include(_self|replace({ "content.html.twig": "banner.html.twig" })) }}

    {# <div class="container d-flex">
		<h2 class="text-uppercase h5 fw-bolder py-4 me-2">подбор шин</h2>

		<div class="d-flex align-items-center small">
			<div class="small mb-1">
				<span class="text-muted">Свыше</span>
				<a href="{{ path('products-product:user.catalog') }}" class="px-1">3000 моделей</a>

			</div>
		</div>
	</div> #}


    {# Фильтр подбора  #}
    <section  class="selection-box container my-4">
        {{ include(_self|replace({ "content.html.twig": "filter.html.twig" })) }}
    </section>



    {#{% for key, cat in category %}#}


        {#{% if products[key]|length %}#}


            <section class="cards-box shadow pb-3">
                <div class="container">

                    <div class="d-flex  mt-3">
                        <h2 class="text-uppercase h5 fw-bolder py-4 me-3">Автомобильные шины{#{{ cat.category_name }}#}</h2>
                        <div class="d-flex align-items-center">
                            <div class="small mb-1">
                                {#<a href="{{ path('products-product:user.catalog.category', { category : cat.category_url }) }}"
                                   class="small text-lowercase">Все шины</a>#} {#{{ cat.category_name }}#}
                            </div>
                        </div>
                    </div>


                    <div class="cards-box-body d-flex flex-wrap">
                        {#{% for product in products[key] %}#}
                        {% for product in products %}
                            <div class="w-50">
                                {% include Template('/user/mobile/includes/card.html.twig') with { 'card': product } %}
                            </div>
                        {% endfor %}
                    </div>

                    {#<a
                            class="btn btn-primary w-100 rounded-3 text-uppercase fw-bolder my-2"
                            title="Показать еще"
                            href="{{ path('products-product:user.catalog.category', { category : cat.category_url }) }}"
                    >
                        Показать еще
                    </a>#}

                </div>
            </section>

            <section class="mt-4">
                {# ЛИДЕРЫ ПРОДАЖ #}
                <div class="container">

                    <div class="px-2">
                        <h2 class="text-uppercase h5 fw-bolder">Лидеры продаж</h2>
                    </div>

                    <div class="bg-light rounded-4 px-2 py-1 mt-4">
                        {#{% include Template('user/'~ (baks_settings.device ?: 'pc') ~'/includes/leader.html.twig') with { 'leader' : leader[key] } %}#}
                        {% include Template('user/'~ (baks_settings.device ?: 'pc') ~'/includes/leader.html.twig') with { 'leader' : leader } %}
                    </div>
                </div>
            </section>

        {#{% endif %}#}

    {#{% endfor %}#}



    {# АКЦИИ \ СКИДКИ #}
    {# <section id="action" class="pt-4">
		
		<div class="d-flex flex-nowrap gap-3 overflow-auto w-100 p-3">
			{% for i in 0..5 %}
				{{ include(_self|replace({ "content.html.twig": "action.html.twig" })) }}
			{% endfor %}
		</div>
	
	</section> #}



    {# ПРОСМОТРЕННЫЕ #}
    {# <section id="viewed" class="mt-4 bg-light">
		<div class="col-12 p-4">
			<h2 class="text-uppercase me-4 h5 fw-bolder">Просмотренные</h2>
		</div>
		
		<div class="d-flex flex-nowrap gap-4 overflow-auto w-100 px-4 pb-3">
			{% for i in 0..5 %}
    #}{# {{ include(_self|replace({ "content.html.twig": "viewed.html.twig" })) }} #}{#
    {% include('@Template/default/user/mobile/includes/viewed.html.twig') %}
{% endfor %}
</div>
</section> #}





    {# ОТЗЫВЫ #}

    {# <section id="reviews" class="container shadow pb-4">
		
		<div class="container d-flex align-items-stretch py-4">
			<h2 class="text-uppercase me-4 h5 fw-bolder">отзывы</h2>
			<a href="#" class="">Все отзывы</a>
		</div>
		
		<div class="bg-light rounded-4 px-2 py-1">
    #}{# {{ include(_self|replace({ "content.html.twig": "reviews.html.twig" })) }} #}{#
    <ul class="list-group list-group-flush pt-3">
        {% for i in 0..2 %}
            {% include('@Template/default/user/mobile/includes/reviews.html.twig') %}
        {% endfor %}
    </ul>

</div>
</section> #}

    <section id="about" class="container px-4">
        <h2 class="text-uppercase h5 fw-bolder mt-5 mb-3">О компании</h2>

        {{ include(_self|replace({ "content.html.twig": "about.html.twig" })) }}

    </section>

    <section id="maps">
        <h2 class="d-none">ООО «Triangle-шины» на карте</h2>
        {{ include(_self|replace({ "content.html.twig": "maps.html.twig" })) }}
    </section>


</main>




