{% trans_default_domain 'user.reg' %}

{# ---- ШАБЛОН ------------------------------------------------------------ #}
{% extends Template('user/base.html.twig') %}

{% block stylesheets %}
    <meta name="robots" content="none">
{% endblock %}

{% block title %}{{ 'user.page'|trans }}{% endblock %}


{# ---- CSS ------------------------------------------------------------ #}
{# {% block stylesheets %} #}
{#    <link href="{{ path('user_posts_home_css') }}" rel="stylesheet"> #}
{# {% endblock %} #}

{# ---- HEADER-БЛОК ------------------------------------------------------------ #}
{# {% block header %}
    {{ include(_self|replace({ "template.html.twig": "header.html.twig" })) }}
{% endblock %} #}


{# ---- LEFT-БЛОК ------------------------------------------------------------ #}
{# {% block left %} #}
{#   {% include '@Template/flowers/left.html.twig' %} #}
{# {% endblock %} #}


{# ---- КОНТЕНТ-БЛОК ------------------------------------------------------------ #}
{% block content %}
	{{ include(_self|replace({ "template.html.twig" : (baks_settings.device ?: 'pc') ~ "/content.html.twig" })) }}
{% endblock %}


{# ---- FOOTER-БЛОК ------------------------------------------------------------ #}
{# {% block footer %}
    {{ include(_self|replace({ "template.html.twig": "footer.html.twig" })) }}
{% endblock %} #}


 {% block javascript %}
 <script data-src='/assets/js/password.meter.js' class='lazy'></script>
 {% if CAPTCHA is defined %}
 <script nonce="{{ csp_nonce() }}" class="lazy" type="module" data-src="https://cdn.jsdelivr.net/npm/friendly-challenge@0.9.9/widget.module.min.js"></script>
{% endif %}

{#<script nonce="{{ csp_nonce() }}">

        let btnLogin = document.getElementById('registration_form_registration');
        btnLogin.disabled = true;
        btnLogin.type = 'button'

        let progress = document.getElementById('registration_form_registration-progress');
        progress.classList.remove('d-none');


 let regCaptcha = document.getElementById('registration-captcha');
        regCaptcha.classList.remove('d-none');

        function myCallback() {
            btnLogin.disabled = false;
            btnLogin.type = 'submit';


            progress.classList.add('d-none');
            regCaptcha.classList.add('d-none');
        }

        function myCallbackError(error) {
            console.log("Error " + error);
        }

    </script>#}

 {% endblock %}

