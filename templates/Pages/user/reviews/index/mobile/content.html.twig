{# {{ include(_self|replace({ "content.html.twig": "header.html.twig" })) }} #}

<main class="main border-bottom">

	<section id="reviews" class="container">

		<div class="d-flex justify-content-start align-items-center mt-4 gap-3">
			<h1 class="h4 text-uppercase fw-bolder mb-0">Шины Triangle TR652</h1>
		</div>

		{# РЕЙТИНГ #}
		<div class="d-flex gap-1 align-items-center mt-2">
			{% include '@Template/default/user/pc/includes/rating.html.twig' %}
			<a href="#reviews" class="small ms-3" aria-label="Отзывы" title="Отзывы">9 отзывов</a>
		</div>

		<div class="py-3 text-center">
			<a href="#" class="btn btn-danger w-100 text-uppercase mb-3"
			   title="Добавить отзыв"
			   aria-label="Добавить отзыв">
				Добавить отзыв
			</a>
		</div>

		<div class="d-flex flex-column gap-3 mt-3">
			{% for i in 0..5 %}
				{#{{ include(_self|replace({ "content.html.twig": "comment.html.twig" })) }}#}
				{% include('@Template/default/user/mobile/includes/review.html.twig') %}
			{% endfor %}
		</div>


		<div class="py-3 text-center">

			<div class="py-3 text-center">
				<a href="#" class="btn btn-primary w-100 text-uppercase mb-3"
				   title="Показать еще"
				   aria-label="Показать еще">
					Показать еще
				</a>
			</div>


			<a href="{{ path('Pages:user.homepage') }}" class="small w-100 text-muted p-3">
				Продолжить покупки
			</a>

		</div>

	</section>

</main>




