let $addButton=document.getElementById("delivery_transport_form_add_driver");if($addButton){let $blockCollection=document.getElementById("delivery_transport_form-collection");if($blockCollection){$addButton.addEventListener("click",function(){let $addButton=this;let newForm=$addButton.dataset.prototype;let index=$addButton.dataset.index*1;newForm=newForm.replace(/__delivery_driver__/g,index);let div=document.createElement("div");div.id="item_delivery_transport_form_driver_"+index;div.innerHTML=newForm;$blockCollection.append(div);new NiceSelect(div.querySelector('[data-select="select2"]'),{searchable:true});div.querySelector(".del-item")?.addEventListener("click",deleteItem);$addButton.dataset.index=(index+1).toString()})}}document.querySelectorAll(".del-item").forEach(function(item){item.addEventListener("click",deleteItem)});function deleteItem(){console.log(this.dataset.delete);document.getElementById(this.dataset.delete).remove()}